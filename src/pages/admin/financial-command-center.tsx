import { useEffect,useState } from 'react';
import { AdminLayout } from './dashboard';
import { supabase } from '@/lib/supabase';
import { formatKES } from '@/lib/utils';
export default function FinancialCommandCenter(){const [d,setD]=useState<any>(null);const [loading,setLoading]=useState(true);useEffect(()=>{supabase.rpc('get_business_analytics',{p_days:30}).then(({data})=>setD(data)).finally(()=>setLoading(false));},[]);return <AdminLayout title="Financial Command Center"><div className="grid md:grid-cols-2 xl:grid-cols-4 gap-4">{[['Sales',d?.sales],['Invoiced',d?.invoiced],['Collected',d?.collected],['Outstanding',d?.outstanding]].map(([l,v])=><div key={l as string} className="bg-white border rounded-xl p-5"><p className="text-sm text-gray-500">{l}</p><p className="text-2xl font-bold mt-1">{loading?'—':formatKES(Number(v||0))}</p></div>)}</div><div className="mt-6 bg-white border rounded-xl p-5"><h2 className="font-bold">30-Day Operating Snapshot</h2><div className="grid md:grid-cols-3 gap-4 mt-4"><div><p className="text-sm text-gray-500">Website visits</p><p className="text-xl font-semibold">{d?.visits??'—'}</p></div><div><p className="text-sm text-gray-500">Orders</p><p className="text-xl font-semibold">{d?.orders??'—'}</p></div><div><p className="text-sm text-gray-500">Unique pages</p><p className="text-xl font-semibold">{d?.unique_paths??'—'}</p></div></div></div></AdminLayout>}
